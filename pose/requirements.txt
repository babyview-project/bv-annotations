# Known-good env for the pose pipeline on ccn2 (A40, CUDA 12.x driver).
# The hard constraint: mmcv 2.1.0 must match torch 2.1.x — install mmcv from the
# openmmlab prebuilt wheel index (see SETUP.md), NOT via pip/mim source build.
torch==2.1.0+cu121
torchvision==0.16.0+cu121
numpy==1.26.4          # <2 required (torch 2.1 + mm* were built against numpy 1.x)
mmengine==0.10.7
mmcv==2.1.0
mmdet==3.3.0
mmpose==1.3.2
xtcocotools==1.14.3
ultralytics==8.4.87    # YOLO12x person detector
ray==2.56.0
opencv-python==5.0.0.93
